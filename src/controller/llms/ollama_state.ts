// TODO
import { MessageContent } from '../../types/extendWindow/chat'

const fetchMessages = (messageId, limit) => {
  const result: MessageContent[] = []
  return result
}

const storeMessages = (messages, messageId): void => {}

export { fetchMessages, storeMessages }
